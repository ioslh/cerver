#ifndef cerver_h
#define cerver_h

#include "utils.h"
#include "sbuf.h"
#include "rio.h"

#define MIN(x,y) ((x) < (y) ? (x) : (y))
#define MAX(x,y) ((x) > (y) ? (x) : (y))
#define CRLF "\r\n"
#define SERVER_NAME "Cerver"
#define NTHREADS 8


#endif /* cerver_h */
